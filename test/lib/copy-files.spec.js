var copyFiles = require('../../lib/copy-files');
var sinon     = require('sinon');
var fs        = require('fs');

describe('#copyFiles', function() {
  it('should exist', function() {
    copyFiles.should.be.ok;
  });

  it('should copy copiable file to target', function() {

  });
});
