'use strict';

var poontang = require('../');
var sinon    = require('sinon');
var should   = require('should');

describe('poontang', function () {

  it('should exist', function () {
    poontang.should.be.ok;
  });

});
